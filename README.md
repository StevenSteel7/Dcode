To run the extension
`cd webpack`
`npm install`
`npx webpack`

Easy Peasy

main: the main organisational folder
    |-> inside the main oragisational folder, we have layout.tsx for protected routes(or just use middleware)
We also used mediaQuerry hook for conditional rendering of components

